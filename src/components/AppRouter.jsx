import React from 'react';
import ImageUpload from './ImageUpload';

const AppRouter = () => {
  // Directly render the ImageUpload component as it's the only page now
  return <ImageUpload />;
};

export default AppRouter; 